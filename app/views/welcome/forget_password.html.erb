<div class="reveal-modal custom-modal" id="forget-password" data-reveal data-close-on-click="true" data-animation-in="fade-in" data-animation-out="fade-out">
  <h2>忘記密碼了？輸入電子郵件寄送密碼重設資訊！</h2>
  <div class="alert_info"></div>

  <%= simple_form_for Member.new, as: :member, url: member_password_path, method: :post, html: { id: "forget_password_form" }, remote: true, format: :json do |f| %>
    <%= f.input :email %>
    <div class="form-actions">
      <%= f.button :submit, "寄送密碼重置指示" %>
    </div>
  <% end %>

  <button class="close-button" data-close aria-label="Close reveal" type="button">
    <span aria-hidden="true">&times;</span>
  </button>
</div>